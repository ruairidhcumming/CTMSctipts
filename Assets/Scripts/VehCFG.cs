using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VehCFG : MonoBehaviour
{
    public float sightRaduis = 20;
    //initilise movement 
    public float hp = 100;
    public float str = 10;
    // Start is called before the first frame update
    void Start()
    {
        
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
